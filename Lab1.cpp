#include <iostream>
3include <vector>
#include "workers.h"
using namespace std;

int main() {
	vector <worker>
	string workerName;
	int workerId;
	
	for(i=0; i<4; i++)
	{
		cout<< "Enter a workeer name";
		cin >> workerName;
		cout<< "Enter the worker id";
		cin >> workerId;
		
		worker account
		
		account.getName("workerName")
		account.setId("workerId")
		workerInfo.push_back(account);
	}
	
	for (int j =0; j < workerInfo.size(); j++)
	{
		workerInfo.printAll();
		
	}
}	
